TAVILY_API_KEY=
# Optional: raises GitHub API rate limits for scrape_github.py (10 req/min -> 30 req/min
# for search, 60 req/hour -> 5000 req/hour for core endpoints). No scopes required for
# public data.
GITHUB_TOKEN=
